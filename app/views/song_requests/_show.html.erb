<%= song_request.input %>

<%= simple_form_for [song_request, Comment.new] do |f| %>
  <%= f.input :input %>
  <%= f.submit "add a comment", class: "btn btn-primary" %>
<% end %>



<ul>
  <%= render partial: 'comments/comment', collection: song_request.comments %>
</ul>

